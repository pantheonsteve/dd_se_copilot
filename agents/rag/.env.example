# RAG Agents — Environment Configuration
# Copy this file to .env and fill in your values.
# The root .env is also loaded; values here take precedence.

OPENAI_API_KEY=sk-...                          # Required — embeddings (text-embedding-3-small)
ANTHROPIC_API_KEY=sk-ant-...                   # Required — Claude LLM for RAG answers
